{
  "name": "g0p-ai",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx --yes serve .",
    "build": "mkdir -p public && cp index.html public/index.html",
    "test": "node --test test/*.test.mjs"
  }
}
